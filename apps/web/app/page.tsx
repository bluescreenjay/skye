// Placeholder only. This is the API host shell, not the product Home
// (Home lives in the extension; see constitution Principle VII).
export default function Page() {
  return (
    <main>
      AI Browser API shell — persistence lives at /api/* (feature 003). Product
      Home is not this page.
    </main>
  );
}

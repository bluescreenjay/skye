# @ai-browser/web

Next.js (App Router) shell. It will host the workspace API and server-side
intelligence; it is not the in-tab workspace UI.

Currently a placeholder that imports the domain types from
`@ai-browser/shared`. Workspace persistence routes land in feature 003 and
AI clustering in feature 004.

/** Sidebar-facing re-exports of shared correction helpers (feature 007). */
export {
  ignoreSuggestion,
  listPendingSuggestions,
  listWorkspaces,
  moveTab,
  putTabMembership,
} from "../corrections/api";

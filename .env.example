# Copy to .env in the repository root (copy, do not rename) and fill in.
# Never commit .env or real keys.
#
# apps/web reads DATABASE_URL and DEVICE_TOKEN_SECRET (feature 003).
# The Chrome extension has its own file: apps/extension/.env.example.
# The other keys are documented here for later features.

# Postgres connection.
# Preferred: Tiger Data (Timescale Postgres)
#   postgres://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require
# Pivot: Supabase project connection string (keep sslmode=require)
# apps/web adds sslmode=require when the URL has no sslmode, so a local Postgres
# without TLS needs an explicit ?sslmode=disable.
DATABASE_URL=

# Secret used to hash device tokens. Any long random string, kept private and
# stable: changing it makes every existing device token map to a new user.
DEVICE_TOKEN_SECRET=

# LLM for clustering (feature 004) and chat / actions (later features).
# Preferred: Google Gemini. Pivot: set ANTHROPIC_API_KEY or OPENAI_API_KEY instead.
GEMINI_API_KEY=
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# Gemini model id. Default: gemini-3.5-flash-lite (verified callable 2026-09-19 and
# chosen for its higher free-tier rate limits). Note: gemini-2.5-flash is listed by
# the API but returns 404 for new users; a listed model is not necessarily callable.
# GEMINI_MODEL=gemini-3.5-flash-lite
# Optional: a different model just for clustering (GEMINI_MODEL_CLUSTER), e.g. gemini-3.6-flash,
# which reported higher confidence on a hard-to-group set than the lite model did.
# GEMINI_MODEL_CLUSTER=
# Thinking effort for model calls: minimal (default, fastest) | low | medium | high.
# GEMINI_THINKING_LEVEL=minimal

# Clustering: a group at or above this confidence (0..1) is applied automatically;
# below it, the group becomes a suggestion the user can accept or ignore.
# CLUSTER_CONFIDENCE_BAR=0.7

# Guardrail on model requests per Pacific-time day, shared by all AI features and
# kept under the free tier's 500. In-memory: it resets when the server restarts.
# LLM_DAILY_CAP=450

# Desktop voice (feature 013, optional). Pivot: omit and use the Web Speech API.
ELEVENLABS_API_KEY=

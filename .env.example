# Copy to .env and fill in. Never commit .env or real keys.
# Feature 001 does not read these at runtime; this file only documents them.

# Postgres connection.
# Preferred: Tiger Data (Timescale Postgres)
#   postgres://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require
# Pivot: Supabase project connection string (keep sslmode=require)
DATABASE_URL=

# LLM for clustering / chat / actions (later features).
# Preferred: Google Gemini. Pivot: set ANTHROPIC_API_KEY or OPENAI_API_KEY instead.
GEMINI_API_KEY=
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# Desktop voice (feature 013, optional). Pivot: omit and use the Web Speech API.
ELEVENLABS_API_KEY=

# Secret used to hash device pairing tokens. Unused until feature 003.
DEVICE_TOKEN_SECRET=
